#include <stdio.h>

int main (){

    int l, b, p ;
      printf("Enter length: ");
      scanf("%d", &l);

      printf("Enter bearth: ");
      scanf("%d", &b);
    
    //    p= 2*(l+b);
      printf("Perimeter of a rectangle is %d", 2*(l+b));

    return 0;
}
